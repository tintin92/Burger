<h1>Burger Lab!</h1>

<h2>Try Burger!</h2>

<ul>
  {{#each burgers}}
  {{#unless devoured}}
  {{> burgers/burg-block eat=true}}
  {{/unless}}
  {{/each}}
</ul>

<h2>Devoured Burgers!</h2>

<ul>
  {{#each burgers}}
  {{#if devoured}}
  {{> burgers/burg-block eat=false}}
  {{/if}}
  {{/each}}
</ul>

<h2>Add a burg</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="devoured">
  </div>

  <div class="form-group">
    <label for="devoured">Ready to devour?</label><br>
    <input type="radio" name="devoured" value="1" checked> Devoured!<br>
    <input type="radio" name="devoured" value="0"> Eat
  </div>

  <button type="submit">Add Burger to Lab</button>
</form>